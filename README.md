## flask-cors-wrap

### requirements

```
> pip install flask
> openssl req -x509 -newkey rsa:4096 -nodes -out cert.pem -keyout key.pem -days 365
```

### running the server

```
> flask server.py
```
